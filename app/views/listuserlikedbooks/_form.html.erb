<%= form_with(model: listuserlikedbook) do |form| %>
  <% if listuserlikedbook.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(listuserlikedbook.errors.count, "error") %> prohibited this listuserlikedbook from being saved:</h2>

      <ul>
        <% listuserlikedbook.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :book_id, style: "display: block" %>
    <%= form.text_field :book_id %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
